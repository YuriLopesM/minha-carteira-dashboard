import styled from 'styled-components';

export const Container = styled.div`
    > select {
        padding: 9px 10px;
        border-radius: 5px;
        margin-left: 8px;
        cursor: pointer;
    }
`;

